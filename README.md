🧾 Controlador Financeiro — Frontend

O Controlador Financeiro com Análise Visual é uma aplicação web desenvolvida em React que permite o controle de receitas e despesas de forma simples e intuitiva.
O sistema realiza o CRUD (Create, Read, Update, Delete) de transações financeiras e apresenta gráficos interativos utilizando a biblioteca Recharts.

🧠 Tecnologias Utilizadas

React — criação da interface de usuário

Axios — comunicação com o backend (API REST)

Recharts — visualização de dados financeiros

Bootstrap / CSS — estilização básica

⚙️ Pré-requisitos

Antes de rodar o projeto, certifique-se de ter instalado:

Node.js
 (versão 16 ou superior)

npm

Além disso, o backend deve estar rodando localmente (geralmente em http://localhost:3001), pois o frontend se comunica com ele via API REST.

🚀 Como Rodar o Projeto

Acesse a pasta do frontend:

cd frontend


Instale as dependências:

npm install


Inicie o servidor de desenvolvimento:

npm start


Acesse no navegador:

http://localhost:3000
